`gallery-1.jpg` through `gallery-4.jpg` are stock photos (Unsplash, free license) used as placeholders in `index.html`'s gallery section. Replace them with real photos of the clinic/facilities when available — keep the same filenames, or update the `src` attributes in `index.html` if you rename them.
